spring:
  datasource:
    url: jdbc:postgresql://localhost/query_dsl_test2?serverTimezone=UTC&useSSL=false
    username: admin
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
